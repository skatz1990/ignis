apiVersion: batch/v1
kind: Job
metadata:
  name: ignis-spill-job
  namespace: ignis-dev
spec:
  backoffLimit: 0
  template:
    spec:
      initContainers:
        - name: fix-log-perms
          image: busybox:latest
          command: ["sh", "-c", "rm -rf /mnt/spark-logs/* && chmod 777 /mnt/spark-logs"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: spark-logs
              mountPath: /mnt/spark-logs
      containers:
        - name: spark
          image: apache/spark:4.0.2-python3
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p /mnt/spark-logs && \
              /opt/spark/bin/spark-submit \
                --master local[4] \
                --driver-memory 1g \
                --conf spark.eventLog.enabled=true \
                --conf spark.eventLog.dir=file:///mnt/spark-logs \
                --conf spark.sql.shuffle.partitions=4 \
                /app/spill_job.py; \
              echo "SPARK_DONE"; sleep 60
          volumeMounts:
            - name: script
              mountPath: /app
            - name: spark-logs
              mountPath: /mnt/spark-logs
      volumes:
        - name: script
          configMap:
            name: spill-job-script
        - name: spark-logs
          hostPath:
            path: /tmp/ignis-spark-logs
            type: DirectoryOrCreate
      restartPolicy: Never